/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package utilities;

import java.io.Reader;

/**
 *
 * @author User
 */
public class FingerPrintScanner {
    public void scanFinger(){
        /*
        try {
            
            Reader reader = Reader.Open(Reader.Type.FINGERPRINT, Reader.Priority.COOPERATIVE);
            
            System.out.println("Place your finger on the scanner...");

            Fid fid = reader.CaptureFid(-1, -1, -1, Reader.CaptureProcessing.EMBEDDED);
            if (fid != null) {
                // Process the captured fingerprint (e.g., save it to a file or send it to a server).
                System.out.println("Fingerprint captured.");
            }
            
            reader.Close();
        } catch (UareUException e) {
            e.printStackTrace();
        }
        */
    }
    
}
